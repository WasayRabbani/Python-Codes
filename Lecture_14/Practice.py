'''

🔹 Strings

1. Take a string input from the user and print its length.

2. Convert a string to uppercase and lowercase.

3.Slice the first 5 characters from a string.

4. Concatenate two strings using +.

5. Replace all spaces in a string with underscores.


🔹 Lists

1. Create a list of 5 items and print the third item.

2. Add an element to the end of a list using append().

3. Insert an element at position 2 using insert().

4. Remove an element using remove().

5. Sort a list using sort() and then reverse it.


🔹 Tuples

1. Create a tuple with 4 items and access the last item using negative indexing.

2. Convert a tuple into a list using list().

3. Check the index of an item in a tuple using .index().

4. Count how many times an item appears in a tuple.


🔹 Sets

1. Create a set with duplicate values and show that duplicates are removed.

2. Add an item to a set using add().

3. Remove an item using remove() or discard().

4. Convert a list with repeated values to a set to remove duplicates.


🔹 Dictionaries

1. Create a dictionary with 3 key-value pairs and print all the keys.

2. Add a new key-value pair to a dictionary.

3. Update the value of an existing key.

4. Remove a key using pop().

5. Use get() to access a key that may not exist.

🔹 Bonus Mixed Questions

1. Take a string and convert it into a list of characters.

2. Take two lists: one with keys, one with values. Combine them into a dictionary.

3. From a dictionary, get all the values and store them in a list.

4. Make a list of 5 numbers. Convert it to a set and print both.

5. Convert a number into a string using str().

6. Find the length of a tuple using len().

7. Use slicing to get every second item from a list.
'''